我将为您创建并运行一个专门的“僵尸文件猎人”脚本 (`zombie_hunter.js`)，以全量扫描项目中的废弃文件。

### 🛡️ 脚本设计与安全机制

1.  **智能路径解析**:
    - **别名支持**: 完美支持 `tsconfig.json` 中配置的 `@/*` 别名，确保能正确追踪到 `src/` 下的文件。
    - **动态导入**: 增加正则覆盖 `import('...')` 语法，防止漏掉懒加载路由。
    - **文件后缀**: 自动补全 `.ts`, `.tsx`, `/index.ts` 等缺省后缀。

2.  **静态资源防误报**:
    - **全格式扫描**: 不仅扫描代码，还会扫描 `.css`, `.json` 以及代码中引用的图片路径。
    - **资源白名单**: 对于无法通过 import 追踪的纯静态资源（如 `public` 文件夹下的内容），默认视为“存活”或单独列出，避免误判。

3.  **非破坏性**:
    - 脚本仅进行 **“只读”** 操作，不修改、不删除任何文件。
    - 输出结果仅作为决策依据。

### 🚀 执行指南 (如何在当前环境安全启动)

1.  **环境检查**: 您当前已处于 `f:\project_kuajing` 项目根目录，且已安装 Node.js 环境。
2.  **脚本生成**: 我将为您生成 `zombie_hunter.js` 文件。
3.  **启动命令**:
    - 在下方的终端窗口中，我将使用命令 `node zombie_hunter.js` 运行脚本。
    - 您也可以随时手动输入该命令重新扫描。

### 📋 后续步骤
运行脚本后，我将根据输出结果，为您整理一份分级处理清单（A类确废/B类疑废/C类核心），供您审阅。