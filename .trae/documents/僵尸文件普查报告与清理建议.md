基于全量扫描结果，我为您整理了《项目文件生存状况普查报告》。本次扫描以 `src/main.tsx` 为根节点，顺藤摸瓜遍历了所有依赖关系。

### 📊 概览
- **总文件数**: 157 个
- **僵尸文件**: 约 80+ 个 (未被任何核心代码引用)
- **状态**: 存在大量历史遗留代码，严重影响项目维护性。

---

### 🗑️ A 类：确定废弃 (建议优先清理)
这些文件与当前 P4 架构完全脱节，且具有明显的旧版本特征。

1.  **后端遗留脚本** (Frontend src 混入后端代码):
    - `src/backend/db.js`, `src/backend/executor.js`, `src/backend/verify.js`
2.  **旧版组件库** (AtomicTask / Portal / Sidebar):
    - `src/components/AtomicTask/*` (全目录)
    - `src/components/Portal/*` (全目录)
    - `src/components/Sidebar/*` (全目录 - 现已使用 `MissionFoundry/components/FoundrySidebar`)
    - `src/components/MainStage/*` (P2 旧舞台)
3.  **测试/临时文件**:
    - `src/test-double-monitor.html`
    - `src/styles/AtomicTaskStyles.css`

### ⚠️ B 类：疑似废弃 (需二次确认)
这些文件看起来有业务含义，但当前并未被 `EditorPage` 或 `LabPage` 调用。

1.  **未挂载的 Hooks**:
    - `src/hooks/useMissionLoader.ts` (任务加载逻辑似乎已迁移至 `useMissionLogic`)
    - `src/hooks/useLabData.ts`, `useLabProtocol.ts`, `useAutoFlow.ts`
2.  **未使用的服务**:
    - `src/services/MissionDecompiler.ts` (DeepSeek 解析服务，当前未连接)
    - `src/services/taskService.ts`
3.  **孤立数据文件**:
    - `src/data/db/skills_library.json`
    - `src/data/db/tasks_registry.json`

### 🛡️ C 类：核心地基 (绝对保留)
虽然脚本标记为未引用，但它们是项目运行的隐形依赖。

1.  **类型定义**:
    - `src/declarations.d.ts` (TypeScript 全局类型)
    - `src/vite-env.d.ts` (如果存在)
2.  **动态/配置资源**:
    - `src/data/missionData.ts` (可能被动态引用)

---

### 💡 建议行动方案
1.  **立即归档 A 类**: 将 A 类文件移动到 `_legacy_archive_20250124` 目录，观察一周无误后删除。
2.  **审查 B 类**: 逐个确认是否需要重新接入 P4 架构，不需要则归档。
3.  **C 类不动**: 保持原状。

是否需要我帮您创建归档目录并**移动 A 类文件**？ (这是最安全的“软删除”方式)