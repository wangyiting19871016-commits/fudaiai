# 🎯 真迹·2026春节特供项目 - 完整架构设计书

## 📋 项目概述

**项目名称**: 真迹·2026春节遗迹  
**项目类型**: H5移动端应用  
**技术架构**: React + TypeScript + P4LAB能力封装  
**目标**: 春节AI头像/贺卡/视频生成系统

---

## 🏗️ 整体架构

### 系统分层

```
┌─────────────────────────────────────┐
│   H5前端层 (Mobile Interface)        │
│   - 主页 (任务矩阵)                  │
│   - 操作页 (AI炼金实验室)            │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│   任务编排层 (Mission Orchestrator)  │
│   - 任务配置 (M1-M5)                 │
│   - 流程控制 (Pipeline)              │
│   - 状态管理                         │
└─────────────────┬───────────────────┘
                  │
┌─────────────────▼───────────────────┐
│   能力封装层 (P4LAB Capabilities)    │
│   - Qwen-VL (视觉理解)               │
│   - LiblibAI FLUX (图像生成)         │
│   - CosyVoice (语音合成)             │
│   - DeepSeek (文案生成)              │
└─────────────────────────────────────┘
```

---

## 📱 页面结构

### 页面 I：主页 [真迹·2026春节遗迹]

**路由**: `/festival/2026/home`

#### 视觉设计
- **配色**: 红金主题（#DC143C + #FFD700）
- **质感**: 毛玻璃效果（Glassmorphism）
- **背景**: 动态粒子烟花动画

#### 核心模块

##### 1. Hero Area
```
┌─────────────────────────────────────┐
│   [真迹·2026]                        │
│   留下你的真迹                       │
│   ────────────────                  │
│   Spring Festival AI Magic          │
└─────────────────────────────────────┘
```

##### 2. 任务矩阵 (Mission Grid)
```
┌─────────┬─────────┐
│  M1 卡片 │  M2 卡片 │
│ 3D头像   │ 全家福   │
├─────────┼─────────┤
│  M3 卡片 │  M4 卡片 │
│ 藏头画   │ 语音贺卡 │
└─────────┴─────────┘
```

**每个卡片包含**:
- 动态预览图/动画
- 任务名称
- 一句话描述
- "开启真迹"按钮

##### 3. 成就墙
```
┌─────────────────────────────────────┐
│ 全国用户实时真迹                     │
│ [图1] [图2] [图3] [图4] ...滚动     │
└─────────────────────────────────────┘
```

---

### 页面 II：操作页 [AI炼金实验室]

**路由**: `/festival/2026/lab/:missionId`

#### 5步交互流程

##### Step 1: 祭坛 (Input)
```
┌─────────────────────────────────────┐
│                                     │
│        📷                            │
│    [上传照片]                        │
│   或 [拍摄照片]                      │
│                                     │
└─────────────────────────────────────┘
```
- 大面积上传区域
- 支持摄像头/相册
- 拖拽上传

##### Step 2: 仪式感 (Loading)
```
┌─────────────────────────────────────┐
│   🧬 DNA 提取中...                   │
│   ────────────────                  │
│   Hair: wavy, medium-length         │
│   Face: oval, mature features       │
│   Age: adult (30-40)                │
│   ────────────────                  │
│   [进度条 ████████░░ 80%]           │
└─────────────────────────────────────┘
```
- 显示Qwen-VL提取的英文标签
- 实时进度
- 增强信任感

##### Step 3: 显影 (Result)
```
┌─────────────────────────────────────┐
│                                     │
│   [模糊 → 清晰的图片显示]            │
│                                     │
└─────────────────────────────────────┘
```
- 渐变显示动画
- 高清大图预览

##### Step 4: 赋能 (Add-ons)
```
┌─────────────────────────────────────┐
│   💬 AI判词                          │
│   "福满人间，春意盎然。"              │
│   - Powered by DeepSeek             │
└─────────────────────────────────────┘
```
- 自动生成专属文案
- 可重新生成

##### Step 5: 扩散 (Output)
```
┌─────────────────────────────────────┐
│ [💾 保存高清图]  [🎙️ 生成语音版]    │
│ [📤 分享朋友圈]  [🔄 重新生成]      │
└─────────────────────────────────────┘
```
- 多种输出选项
- 分享功能

---

## 🎯 任务定义 (M1-M5)

### M1: 3D皮克斯头像 ✅ 已实装
**技术栈**: Qwen-VL-Plus + LiblibAI FLUX + Pixar LoRA  
**输入**: 用户照片  
**输出**: 皮克斯3D风格春节头像  
**用途**: 社交媒体头像、春节贺卡

**配置**:
```json
{
  "mission_id": "M1_3D_PIXAR",
  "api_slot": "liblib-controlnet",
  "model": "liblib-flux-dev",
  "lora_uuid": "95ec78a639394f48827c31adabc00828",
  "prompt_template": "pks, (masterpiece), 3d pixar style, {{DNA}}, wearing red chinese jacket with gold dragon, holding yuanbao"
}
```

---

### M2: 全家福生成
**技术栈**: Qwen-VL-Plus + LiblibAI FLUX  
**输入**: 多张家庭成员照片  
**输出**: 统一风格的春节全家福合成图  
**特色**: 空席位 → 满席位动画

**核心逻辑**:
1. 批量提取每个人的DNA
2. 统一风格渲染
3. 智能排版（横/竖版）
4. 添加春节装饰元素

---

### M3: 藏头画（视觉错觉）
**技术栈**: LiblibAI QRCode ControlNet  
**输入**: 用户文字（4-12字）  
**输出**: 藏有文字的艺术画  
**特色**: 近看是画，远看是字

**用途**: 春节祝福、创意贺卡

---

### M4: 语音贺卡
**技术栈**: DeepSeek (文案) + CosyVoice (语音) + 图片合成  
**输入**: M1/M2生成的图片  
**输出**: 图片 + 语音的动态贺卡  
**特色**: 自动生成拜年词，配音播放

**流程**:
1. DeepSeek生成春节祝福语
2. CosyVoice语音合成
3. 图片+音频合成视频/动态卡片

---

### M5: 待定（备选方案）

**候选1**: AI对联生成  
**候选2**: 春节短视频（Runway Gen3）  
**候选3**: 福字生成器（书法风格）

---

## 🔧 技术实现

### 文件结构
```
src/
├── pages/
│   ├── Festival2026/
│   │   ├── HomePage.tsx          # 主页
│   │   ├── LabPage.tsx            # 操作页
│   │   └── components/
│   │       ├── MissionCard.tsx    # 任务卡片
│   │       ├── AchievementWall.tsx # 成就墙
│   │       ├── InputAltar.tsx     # 祭坛（上传区）
│   │       ├── LoadingRitual.tsx  # 仪式感加载
│   │       └── ResultDisplay.tsx  # 结果展示
├── configs/
│   ├── missions/
│   │   ├── M1_PixarAvatar.json    # M1配置
│   │   ├── M2_FamilyPortrait.json # M2配置
│   │   ├── M3_HiddenText.json     # M3配置
│   │   └── M4_VoiceCard.json      # M4配置
├── services/
│   ├── MissionExecutor.ts         # 任务执行引擎
│   ├── PipelineRunner.ts          # 流程运行器
│   └── DNAExtractor.ts            # DNA提取服务
└── styles/
    └── festival2026.css           # 春节主题样式
```

---

### 路由配置
```typescript
<Route path="/festival/2026" element={<Festival2026Layout />}>
  <Route index element={<HomePage />} />
  <Route path="lab/:missionId" element={<LabPage />} />
</Route>
```

---

## 🎨 UI/UX规范

### 配色方案
```css
--festival-red: #DC143C;
--festival-gold: #FFD700;
--festival-bg: linear-gradient(135deg, #1a1a1a 0%, #2d1b1b 100%);
--glass-bg: rgba(255, 255, 255, 0.1);
--glass-border: rgba(255, 255, 255, 0.2);
```

### 毛玻璃效果
```css
.glass-card {
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 16px;
}
```

### 动画效果
- **粒子烟花**: Canvas/Three.js
- **卡片悬浮**: hover 3D transform
- **图片显影**: opacity 0→1 + blur 20px→0

---

## 🔄 任务执行流程

### Pipeline架构
```typescript
interface MissionPipeline {
  step_1?: VisionStep;      // 可选：视觉理解
  step_2: GenerationStep;    // 必需：生成
  step_3?: EnhanceStep;      // 可选：增强（文案/语音）
}
```

### 执行引擎
```typescript
class MissionExecutor {
  async execute(missionId: string, inputs: any): Promise<Result> {
    const config = loadMissionConfig(missionId);
    
    // Step 1: 视觉理解（如果需要）
    let dna = null;
    if (config.pipeline.step_1) {
      dna = await callQwenVL(inputs.image);
    }
    
    // Step 2: 图像生成
    const image = await callFlux({
      ...config.pipeline.step_2,
      dna: dna
    });
    
    // Step 3: 增强（如果需要）
    if (config.pipeline.step_3) {
      const caption = await callDeepSeek(image);
      return { image, caption };
    }
    
    return { image };
  }
}
```

---

## 📊 任务配置标准格式

### 完整示例 (M1)
```json
{
  "mission_id": "M1_3D_PIXAR",
  "name": "3D皮克斯",
  "description": "皮克斯风格春节头像",
  "icon": "🎭",
  "preview_images": [
    "/assets/m1-preview-1.png",
    "/assets/m1-preview-2.png"
  ],
  "pipeline": {
    "step_1": {
      "type": "vision",
      "api_slot": "qwen-dashscope",
      "model": "qwen-vl-plus",
      "prompt": "Extract hair topology, face geometry, age anchor. Output English tags only.",
      "output_var": "identity_tags"
    },
    "step_2": {
      "type": "generation",
      "api_slot": "liblib-controlnet",
      "model": "liblib-flux-dev",
      "params": {
        "template_uuid": "5d7e67009b344550bc1aa6ccbfa1d7f4",
        "lora_uuid": "95ec78a639394f48827c31adabc00828",
        "lora_weight": 0.8,
        "sampler": 15,
        "steps": 25,
        "cfg_scale": 3.5,
        "width": 768,
        "height": 1024
      },
      "prompt_template": "pks, (masterpiece:1.2), 3d pixar animation style, ({{identity_tags}}:1.7), (detailed hair strands, clear forehead:1.4), {{GENDER_MOD}}, wearing vibrant red traditional Chinese silk jacket with gold dragon patterns, holding shiny golden ingot (Yuanbao), soft cinematic lighting, bokeh festive background, high-end 3d character design, rendered in Octane, stylized movie look, vibrant colors, clean smooth surfaces",
      "negative_prompt": "{{GENDER_NEG}}, snake, reptile, low quality, distorted",
      "gender_logic": {
        "male": {
          "modifier": "(adult masculine male, sharp mature features, clean ears:1.5)",
          "negative_add": "earrings, tassels, jewelry, female, makeup, lipstick, feminine, baby-face, puffy hair"
        },
        "female": {
          "modifier": "(elegant young adult woman, refined sophisticated features:1.4)",
          "negative_add": "beard, mustache, rough skin, masculine"
        }
      }
    },
    "step_3": {
      "type": "caption",
      "api_slot": "n1n-ultimate",
      "model": "deepseek-ai/DeepSeek-V3",
      "prompt_template": "根据这张皮克斯风格的春节头像，生成一句8-12字的吉祥话，要求：押韵、喜庆、有文化底蕴。只输出文案，不要解释。"
    }
  },
  "ui_config": {
    "primary_color": "#DC143C",
    "accent_color": "#FFD700",
    "loading_text": "DNA提取中...",
    "success_text": "真迹已显影"
  }
}
```

---

## 🛠️ 实现计划

### Phase 1: 基础架构 (2-3天)
1. ✅ P4LAB能力层已完成
   - LiblibAI FLUX + LoRA
   - Qwen-VL
   - 其他API集成

2. 创建H5页面框架
   - 主页组件
   - 操作页组件
   - 路由配置

3. 任务配置系统
   - JSON配置文件
   - 任务加载器
   - 参数验证

---

### Phase 2: 任务实装 (3-4天)
1. M1: 3D皮克斯头像
   - ✅ 技术验证完成
   - 封装为可调用配方
   - 性别识别逻辑

2. M2: 全家福生成
   - 多图批量处理
   - DNA融合算法
   - 智能排版

3. M3: 藏头画
   - QRCode ControlNet
   - 文字转图像

4. M4: 语音贺卡
   - 文案生成
   - 语音合成
   - 多媒体组合

---

### Phase 3: UI/UX (2-3天)
1. 主页实现
   - 任务矩阵动画
   - 成就墙滚动
   - 粒子背景

2. 操作页实现
   - 5步流程UI
   - 加载动画
   - 结果展示

3. 交互优化
   - 手势操作
   - 反馈动效
   - 错误处理

---

## 📦 P4能力库配方（Recipe）

### 配方结构
```typescript
interface P4Recipe {
  id: string;
  name: string;
  mission_id: string;
  slotId: string;
  modelId: string;
  parameters: {
    template_uuid?: string;
    lora_uuid?: string;
    lora_weight?: number;
    prompt_template: string;
    negative_prompt?: string;
    [key: string]: any;
  };
  pipeline?: {
    pre_process?: string[];   // 前置处理（如DNA提取）
    post_process?: string[];  // 后置处理（如文案生成）
  };
}
```

### M1配方示例
```typescript
{
  id: 'recipe-m1-pixar',
  name: '3D皮克斯',
  mission_id: 'M1_3D_PIXAR',
  slotId: 'liblib-controlnet',
  modelId: 'liblib-flux-dev',
  parameters: {
    template_uuid: '5d7e67009b344550bc1aa6ccbfa1d7f4',
    lora_uuid: '95ec78a639394f48827c31adabc00828',
    lora_weight: 0.8,
    sampler: 15,
    steps: 25,
    cfg_scale: 3.5,
    width: 768,
    height: 1024,
    prompt_template: '完整模板...'
  },
  pipeline: {
    pre_process: ['qwen-vl-dna-extraction'],
    post_process: ['deepseek-caption-generation']
  }
}
```

---

## 🚀 开发优先级

### 立即开始（高优先级）
1. **创建Festival2026页面结构**
2. **实现M1任务的完整流程**（作为示例）
3. **创建任务执行引擎**

### 后续实现（中优先级）
4. 实现M2-M4任务
5. UI动画效果
6. 成就墙和分享功能

### 优化阶段（低优先级）
7. 性能优化
8. 错误处理
9. 埋点统计

---

## 🎯 下一步行动

### 立即创建的文件
1. `src/pages/Festival2026/HomePage.tsx` - 主页
2. `src/pages/Festival2026/LabPage.tsx` - 操作页
3. `src/services/MissionExecutor.ts` - 任务引擎
4. `src/configs/missions/M1_PixarAvatar.json` - M1配置
5. `src/styles/festival2026.css` - 春节主题样式

---

## ✅ 当前状态

- ✅ LiblibAI FLUX + LoRA 已验证可用
- ✅ M1任务技术栈已打通
- ✅ P4LAB能力层已完备
- 🔄 等待用户确认方案后开始实装

---

## 🤔 需要确认的问题

1. **M2-M5任务是否按照我的定义？还是有其他需求？**
2. **H5页面是否需要响应式设计（适配不同手机尺寸）？**
3. **成就墙的数据从哪来？需要后端支持吗？**
4. **是否需要用户登录系统？**
5. **分享功能是否需要生成海报图？**

**确认后我立即开始实装！**
