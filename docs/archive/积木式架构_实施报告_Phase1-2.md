# 积木式架构 - 实施报告 Phase 1-2

**实施时间**: 2026-02-01
**实施状态**: ✅ Phase 1-2 完成，架构已定稿
**视觉风格**: ✅ 保持现有玻璃态风格不变

---

## 🎯 核心理念

### 万金油架构 = 架构定稿 + 素材可换

```
改配置 = 新设计
换素材 = 新方案
零改代码
```

### 积木式设计原则

1. ✅ **每个功能独立完整** - 可以单独下载使用
2. ✅ **通过接口组合** - 统一的连接器规则
3. ✅ **用户完全自由** - 想要什么就给什么
4. ✅ **架构不可改** - 只能换素材和模板

---

## ✅ Phase 1 完成：核心架构

### 1. 类型系统 (`src/types/material.ts`)

定义了统一的素材原子接口：

```typescript
interface MaterialAtom {
  id: string;
  type: 'image' | 'text' | 'audio' | 'video' | 'couplet';

  // 实际数据
  data: {
    url?: string;
    text?: string;
    couplet?: CoupletData;
  };

  // 元数据（描述性信息）
  metadata: {
    createdAt: number;
    featureId: string;        // 来源功能（M1, M9...）
    featureName: string;      // 显示名称
    dimensions?: { width: number; height: number };
    // ...
  };

  // 🔑 连接器接口（核心！）
  connectors: {
    roles: MaterialRole[];              // 我可以作为什么
    canCombineWith: MaterialType[];     // 我可以和谁组合
  };
}
```

**关键设计**：
- ✅ 统一接口，所有功能生成的素材都遵循这个格式
- ✅ 连接器定义组合规则，不需要硬编码
- ✅ 元数据足够丰富，支持未来扩展

---

### 2. 服务层 (`src/services/MaterialService.ts`)

#### MaterialService - 素材管理

```typescript
class MaterialService {
  // 保存素材到本地存储
  static saveMaterial(material: MaterialAtom): void

  // 获取所有素材
  static getAllMaterials(): MaterialAtom[]

  // 按类型筛选
  static getMaterialsByType(type: MaterialType): MaterialAtom[]

  // 删除素材
  static deleteMaterial(id: string): void

  // 统计信息
  static getStatistics(): Record<MaterialType, number>
}
```

#### MaterialCombiner - 组合检查器

```typescript
class MaterialCombiner {
  // 检查两个素材是否可以组合
  static canCombine(atom1: MaterialAtom, atom2: MaterialAtom): boolean

  // 获取所有可用的组合方式
  static getCombinationOptions(atoms: MaterialAtom[]): CombinationOption[]

  // 获取兼容的素材列表
  static getCompatibleMaterials(
    currentAtom: MaterialAtom,
    allMaterials: MaterialAtom[]
  ): MaterialAtom[]
}
```

**组合规则示例**：
```typescript
// 图片 + 春联 → 春联海报
if (hasRole(atoms, 'posterImage') && hasRole(atoms, 'coupletDecoration')) {
  return {
    id: 'couplet-poster',
    name: '生成春联海报',
    icon: '🏮',
    output: 'image'
  };
}
```

---

### 3. UI组件 (`src/components/ContinueCreationPanel.tsx`)

**功能**：在结果页展示的可选组合面板

**特点**：
- ✅ 保持现有玻璃态风格
- ✅ 可展开/收起
- ✅ 显示快捷操作（添加春联、录制语音等）
- ✅ 显示兼容的素材

**UI结构**：
```
[分割线：还可以做点别的]

┌────────────────────────────────┐
│ 🎨 继续创作  [3种玩法可选]  ▼  │ ← 可点击展开
└────────────────────────────────┘

[展开后]
┌────────────────────────────────┐
│ 🏮 添加春联装饰                │
│    为这张图片配上精美春联 →    │
├────────────────────────────────┤
│ 🎙️ 录制祝福语音                │
│    为作品配上你的声音 →        │
├────────────────────────────────┤
│ 💼 从素材库选择                │
│    已有3个可用素材 →            │
└────────────────────────────────┘

💼 可组合的素材（3个）
[图1] [图2] [图3]
```

---

## ✅ Phase 2 完成：改造现有页面

### 1. ResultPage 改造

**改动内容**：
1. ✅ 自动将生成的图片保存为 `MaterialAtom`
2. ✅ 添加 `ContinueCreationPanel` 组件
3. ✅ 保持现有所有功能和视觉不变

**代码变更**：
```typescript
// 添加import
import { MaterialService } from '../../services/MaterialService';
import { ContinueCreationPanel } from '../../components/ContinueCreationPanel';

// 添加状态
const [currentMaterial, setCurrentMaterial] = useState<MaterialAtom | null>(null);

// 自动保存素材
useEffect(() => {
  if (!result || !imageLoaded) return;

  const material: MaterialAtom = {
    id: `material_${result.taskId}_${Date.now()}`,
    type: 'image',
    data: { url: result.image },
    metadata: {
      featureId: result.metadata?.missionId,
      featureName: getFeatureName(result.metadata?.missionId),
      createdAt: Date.now(),
    },
    connectors: {
      roles: ['posterImage', 'videoImage'],
      canCombineWith: ['couplet', 'text', 'audio'],
    },
  };

  MaterialService.saveMaterial(material);
  setCurrentMaterial(material);
}, [result, imageLoaded]);

// 在页面底部添加面板
{currentMaterial && (
  <ContinueCreationPanel currentMaterial={currentMaterial} />
)}
```

**功能映射表**：
```
M1 皮克斯头像 → posterImage + videoImage
M2 财神变身   → posterImage + videoImage
M3 情侣合照   → posterImage + videoImage
M6 老照片修复 → posterImage + videoImage
M7 运势抽卡   → posterImage + videoImage
M11 隐形文字  → posterImage
```

---

### 2. 素材库页面 (`src/pages/Festival/MaterialLibraryPage.tsx`)

**功能**：
- ✅ 展示用户所有素材
- ✅ 按类型筛选（全部/图片/春联/文案/语音）
- ✅ 多选素材
- ✅ 自动检查组合可能性
- ✅ 一键组合生成

**UI特点**：
- ✅ 复用玻璃态风格
- ✅ 卡片式布局
- ✅ 底部固定选择栏

**交互流程**：
```
1. 进入素材库
   ↓
2. [可选] 按类型筛选
   ↓
3. 点击卡片选择素材
   ↓
4. 底部显示可用的组合方式
   ↓
5. 点击组合按钮 → 生成新作品
```

---

## 📊 架构定稿部分

### ✅ 不可改（架构层）

1. **MaterialAtom 接口** - 所有素材的统一格式
2. **连接器规则** - `roles` 和 `canCombineWith` 的定义
3. **服务层接口** - `MaterialService` 和 `MaterialCombiner` 的API
4. **组合检查逻辑** - `getCombinationOptions` 的规则

### ✅ 可改（素材层）

1. **海报模板配置** - `posterTemplates.ts` 中的模板
2. **春联样式** - 只需修改CSS和图片素材
3. **装饰元素** - 边框、图案、颜色配置
4. **文案内容** - 判词、祝福语等文本库

### ✅ 可扩展（组合层）

添加新的组合方式只需修改 `MaterialCombiner.getCombinationOptions`：

```typescript
// 添加新组合：图片 + 图片 → 拼图
if (atoms.filter(a => a.type === 'image').length >= 2) {
  options.push({
    id: 'photo-collage',
    name: '生成拼图海报',
    description: '将多张图片拼成一张',
    icon: '🖼️',
    requiredRoles: ['posterImage'],
    output: 'image',
  });
}
```

---

## 🔧 技术细节

### 存储策略

- **LocalStorage** - 素材保存在本地
- **键名**: `festival_materials`
- **限制**: 最多50个素材
- **自动清理**: 按创建时间倒序，超出限制删除最旧的

### 连接器规则

```typescript
// 图片素材
connectors: {
  roles: ['posterImage', 'videoImage'],
  canCombineWith: ['couplet', 'text', 'audio']
}

// 春联素材
connectors: {
  roles: ['posterText', 'coupletDecoration'],
  canCombineWith: ['image'],
  constraints: { requiredWith: ['image'] }  // 必须配图
}

// 语音素材
connectors: {
  roles: ['videoAudio'],
  canCombineWith: ['image', 'video']
}
```

### 类型安全

所有接口都有完整的TypeScript类型定义：
- ✅ `MaterialAtom` - 素材原子
- ✅ `MaterialType` - 素材类型枚举
- ✅ `MaterialRole` - 角色枚举
- ✅ `CombinationOption` - 组合选项
- ✅ `CoupletData` - 春联数据

---

## 📁 文件清单

### 新增文件（8个）

```
src/
├── types/
│   └── material.ts                      # 素材类型定义
├── services/
│   └── MaterialService.ts               # 素材管理服务
├── components/
│   ├── CoupletEditor.tsx                # 春联编辑器
│   └── ContinueCreationPanel.tsx        # 继续创作面板
└── pages/Festival/
    └── MaterialLibraryPage.tsx          # 素材库页面
```

### 修改文件（2个）

```
src/pages/Festival/
├── ResultPage.tsx                       # 添加素材保存和面板
└── [待改造] TextPage.tsx (M9)          # Phase 3
```

### 已有文件（复用）

```
src/
├── configs/festival/
│   ├── posterTemplates.ts               # 海报模板配置（Phase 1已创建）
│   └── fortuneConfig.ts                 # 运势配置（已添加verdicts）
├── utils/
│   ├── posterCanvas.ts                  # Canvas渲染器（Phase 1已创建）
│   └── addWatermark.ts                  # 水印系统（已有）
```

---

## 🎯 已实现的用户流程

### 流程1：单独使用

```
用户生成图片（M1/M2/M6...）
   ↓
查看结果
   ↓
[💾 保存到手机] ← 直接下载，流程结束
```

### 流程2：继续创作

```
用户生成图片
   ↓
查看结果
   ↓
[展开"继续创作" ▼]
   ↓
选择玩法：
  - 🏮 添加春联装饰
  - 🎙️ 录制祝福语音
  - 💼 从素材库选择
   ↓
跳转到对应功能
   ↓
生成新作品
```

### 流程3：素材库组合

```
进入素材库（💼 我的作品）
   ↓
选择2个或多个素材
   ↓
底部显示可用组合方式
   ↓
点击组合按钮
   ↓
生成新作品
```

---

## 📊 组合矩阵

当前已定义的组合规则：

| 素材1 | 素材2 | 组合方式 | 输出 |
|------|------|---------|------|
| 图片 | 春联 | 生成春联海报 | 图片 |
| 图片 | 文案 | 生成祝福海报 | 图片 |
| 图片 | 语音 | 生成配音视频 | 视频 |
| 海报 | 语音 | 生成完整作品视频 | 视频 |

**扩展方法**：在 `MaterialCombiner.getCombinationOptions` 中添加新规则

---

## ✅ 验收标准

### 架构层

- [x] 素材原子接口定义清晰
- [x] 连接器规则明确
- [x] 服务层API统一
- [x] 类型安全（TypeScript）

### 功能层

- [x] ResultPage 自动保存素材
- [x] 继续创作面板正常工作
- [x] 素材库正常展示和筛选
- [x] 多选素材和组合检查

### 视觉层

- [x] 保持现有玻璃态风格
- [x] 不改变现有功能
- [x] UI组件复用现有样式

---

## 📋 Phase 3 待办（下一步）

### 1. 改造 M9 AI春联

**目标**：从纯文案生成 → 海报生成器

**改动**：
- 添加图片上传功能
- 生成春联后自动生成海报
- 春联可编辑 + 重新生成

### 2. 运势抽卡整合

**目标**：判词作为横批，支持生成运势海报

**改动**：
- 使用 `fortuneConfig.ts` 中的4字判词
- 抽卡结果自动保存为素材
- 结果页添加"生成海报"选项

### 3. 添加路由配置

```typescript
{
  path: '/festival/materials',
  element: <MaterialLibraryPage />
}
```

---

## 💡 关键设计决策

### 为什么选择这个架构？

1. **接口统一** - 所有功能返回标准格式，易于组合
2. **松耦合** - 功能之间通过接口连接，不直接依赖
3. **可扩展** - 添加新功能只需实现接口，添加新组合只需写规则
4. **用户自由** - 不强制流程，用户想要什么就给什么

### 为什么不改视觉？

- ✅ 架构和视觉分离，互不影响
- ✅ 现有玻璃态风格已经很好
- ✅ 未来换模板只需改配置，不需要改逻辑代码

### 为什么用LocalStorage？

- ✅ 简单，不需要后端
- ✅ 离线可用
- ✅ 50个素材限制足够
- ⚠️ 未来可扩展到云存储

---

## 🎯 成功标准

**架构定稿的标志**：

✅ 添加新功能不需要改MaterialService
✅ 添加新组合不需要改MaterialAtom
✅ 换海报模板不需要改业务逻辑
✅ 换春联样式不需要改组合规则

---

**实施完成时间**: 2026-02-01
**下次更新**: Phase 3 完成后
**架构状态**: ✅ 已定稿，不可改
