# 福袋AI填空式海报设计规范

**创建时间**: 2026-02-01
**基于**: React6设计草案 + 福袋AI品牌设计系统
**目标**: 创建一个好看的边框把图片包裹进来的填空式海报

---

## 🎯 设计理念

### 核心概念
**"整体包裹式设计"** - 边框装饰与内容融为一体，而非元素分离

```
传统分离式（❌不采用）:          整体包裹式（✅采用）:
┌────────────────┐            ┌──────────────────┐
│  [灯笼] [灯笼]  │            │  精美边框装饰     │
│  [对联] [对联]  │            │  ┌──────────┐   │
│  [图片]         │   VS       │  │          │   │
│  [文字]         │            │  │   图片   │   │
│  [二维码]       │            │  │          │   │
└────────────────┘            │  └──────────┘   │
                              │  文字 + 二维码   │
                              └──────────────────┘
```

---

## 🎨 视觉风格

### 配色方案（使用我们的品牌色）

```css
/* 主色系 */
--主红色: var(--cny-red-500)  = #E53935
--金色:   var(--cny-gold-500) = #FFD700
--背景:   var(--cny-bg-warm)  = #FFF8F0

/* 参考草案的配色（仅作对比） */
--草案红色: #E63946
--草案金色: #D4AF37

/* 结论：使用我们的品牌色，不照搬草案 */
```

---

## 📐 布局规范

### Canvas尺寸
```
宽度: 750px (@1x) / 1500px (@2x)
高度: 1334px (@1x) / 2668px (@2x)
比例: 9:16 (iPhone标准竖屏)
```

### 分区设计（5个区域）

```
┌─────────────────────────────┐  0px
│  1️⃣ 顶部装饰区               │
│     [灯笼] [横批] [灯笼]      │  120px
├─────────────────────────────┤
│  2️⃣ 对联 + 主图区             │
│                             │
│  [对] ┌───────────┐ [对]    │
│  [联] │           │ [联]    │
│  [左] │   主图    │ [右]    │
│       │ (填充区)  │         │
│       │           │         │
│       └───────────┘         │  900px
├─────────────────────────────┤
│  3️⃣ 文字填充区               │
│     标题（动态填充）          │
│     副标题（动态填充）        │  1080px
├─────────────────────────────┤
│  4️⃣ 底部信息区               │
│  [Logo文字]      [二维码]    │  1230px
├─────────────────────────────┤
│  5️⃣ 安全边距                 │  1334px
└─────────────────────────────┘
```

---

## 🔧 区域详细规范

### 1️⃣ 顶部装饰区（0-120px）

#### 灯笼（左右对称）
```
位置:
  - 左灯笼: x=40px, y=30px
  - 右灯笼: x=670px, y=30px

尺寸: 60px × 80px（包含挂绳）

样式:
  - 灯笼主体: 圆形渐变 (#FF6B6B → #D90429)
  - 挂绳: 1px细线，颜色 rgba(139, 0, 0, 0.3)
  - 文字: "福" 和 "春"，白色，24px bold
  - 发光效果: blur 40px, opacity 0.3
```

#### 横批
```
位置: x=375px (居中), y=60px

内容: "五福临门" / "万事如意" 等（动态）

样式:
  - 背景: 白色半透明 rgba(255,255,255,0.9)
  - 边框: 1px solid rgba(229, 57, 53, 0.2)
  - 圆角: 999px (胶囊形)
  - 内边距: 12px 32px
  - 字体: 20px bold，颜色 #E53935
  - 字间距: 0.15em
  - 装饰点: 左右各一个红点 (6px圆形)
```

---

### 2️⃣ 对联 + 主图区（120-900px）

#### 🔑 核心设计原则
**主图与对联的空间分配 - 解决遮挡问题**

```
总宽度: 750px
分配:
  - 左边距: 60px (8%)
  - 左对联: 50px (6.6%)
  - 左间隙: 15px (2%)
  - 主图区: 500px (66.6%) ⬅️ 核心区域
  - 右间隙: 15px (2%)
  - 右对联: 50px (6.6%)
  - 右边距: 60px (8%)

结论: 主图有500px的安全宽度，对联不会遮挡
```

#### 左对联
```
位置: x=75px, y=180px
尺寸: 50px × 600px

样式:
  - 背景: 白色半透明 rgba(255,255,255,0.95)
  - 边框: 1px solid rgba(229, 57, 53, 0.1)
  - 圆角: 25px (上下圆角)
  - 文字: 垂直排列，18px bold
  - 文字颜色: #212121
  - 字间距: 12px
  - 底部印章: 16px × 16px，红色边框
```

#### 主图填充区
```
位置: x=125px, y=150px (居中对齐)
尺寸: 500px × 650px

样式:
  - 圆角: 32px
  - 阴影: 0 20px 60px rgba(0,0,0,0.15)
  - 边框: 4px solid white (外边框增加质感)
  - 背景: 白色（图片未加载时）
  - 图片填充方式: object-fit: cover（填满）

占位状态:
  - 虚线边框: 2px dashed #E0E0E0
  - 图标: 相机图标，48px
  - 文字: "点击上传照片"，14px，#9E9E9E
```

#### 右对联
```
位置: x=625px, y=180px
尺寸: 50px × 600px

样式:
  - 背景: 红色渐变 (#E53935 → #B71C1C)
  - 边框: 无
  - 圆角: 25px
  - 文字: 垂直排列，18px bold
  - 文字颜色: white
  - 字间距: 12px
  - 底部装饰: 小星星图标
```

---

### 3️⃣ 文字填充区（900-1080px）

#### 标题区域
```
位置: x=375px (居中), y=930px
最大宽度: 600px

样式:
  - 字体: 28px black（特粗）
  - 颜色: #212121
  - 最大行数: 2行
  - 行高: 1.4
  - 对齐: center
  - 溢出处理: ellipsis

占位状态:
  - 灰色矩形: 600px × 40px
  - 圆角: 8px
  - 背景: #F5F5F5
  - 文字: "标题占位符"，12px
```

#### 副标题区域
```
位置: x=375px (居中), y=1010px
最大宽度: 550px

样式:
  - 字体: 18px regular
  - 颜色: #616161
  - 最大行数: 3行
  - 行高: 1.6
  - 对齐: center
  - 溢出处理: ellipsis

占位状态:
  - 灰色矩形: 500px × 60px
  - 圆角: 8px
  - 背景: #EEEEEE
  - 文字: "副标题占位符"，12px
```

---

### 4️⃣ 底部信息区（1080-1230px）

#### Logo文字
```
位置: x=80px, y=1160px

内容:
  - 第一行: "福袋 AI" + PRO标签
  - 第二行: "Made by福袋AI团队"

样式:
  - 品牌名: 16px bold，#212121
  - PRO标签: 10px，背景 #FFE5E5，文字 #E53935
  - 副文字: 11px，#9E9E9E
```

#### 二维码区域
```
位置: x=590px, y=1130px
尺寸: 100px × 100px

样式:
  - 背景: 白色
  - 圆角: 12px
  - 阴影: 0 4px 12px rgba(0,0,0,0.08)
  - 边框: 1px solid #F5F5F5

占位状态:
  - 二维码图标
  - 下方文字: "扫码体验"，12px，#E53935
```

---

## 🎯 填空区域标注（JSON配置）

```json
{
  "templateId": "festival-couplet-v1",
  "templateName": "福袋AI春节对联海报",
  "version": "1.0",
  "canvasSize": {
    "width": 750,
    "height": 1334
  },
  "background": {
    "color": "#FFF8F0",
    "gradient": "linear-gradient(180deg, #FFF8F0 0%, #FFE5E5 100%)"
  },
  "fillAreas": [
    {
      "id": "horizontal-banner",
      "name": "横批",
      "type": "text",
      "position": { "x": 375, "y": 60, "align": "center" },
      "maxLength": 4,
      "style": {
        "fontSize": 20,
        "fontWeight": "bold",
        "color": "#E53935"
      }
    },
    {
      "id": "couplet-left",
      "name": "左对联",
      "type": "text",
      "position": { "x": 100, "y": 180, "height": 600 },
      "maxLength": 7,
      "direction": "vertical",
      "style": {
        "fontSize": 18,
        "fontWeight": "bold",
        "color": "#212121"
      }
    },
    {
      "id": "main-image",
      "name": "主图",
      "type": "image",
      "position": { "x": 125, "y": 150, "width": 500, "height": 650 },
      "style": {
        "borderRadius": 32,
        "objectFit": "cover",
        "shadow": "0 20px 60px rgba(0,0,0,0.15)"
      }
    },
    {
      "id": "couplet-right",
      "name": "右对联",
      "type": "text",
      "position": { "x": 650, "y": 180, "height": 600 },
      "maxLength": 7,
      "direction": "vertical",
      "style": {
        "fontSize": 18,
        "fontWeight": "bold",
        "color": "#FFFFFF"
      }
    },
    {
      "id": "title-text",
      "name": "标题",
      "type": "text",
      "position": { "x": 375, "y": 930, "width": 600, "align": "center" },
      "maxLines": 2,
      "style": {
        "fontSize": 28,
        "fontWeight": "black",
        "color": "#212121"
      }
    },
    {
      "id": "subtitle-text",
      "name": "副标题",
      "type": "text",
      "position": { "x": 375, "y": 1010, "width": 550, "align": "center" },
      "maxLines": 3,
      "style": {
        "fontSize": 18,
        "color": "#616161"
      }
    },
    {
      "id": "qrcode",
      "name": "二维码",
      "type": "qrcode",
      "position": { "x": 590, "y": 1130, "width": 100, "height": 100 },
      "style": {
        "borderRadius": 12,
        "background": "#FFFFFF"
      }
    }
  ],
  "decorations": [
    {
      "id": "lantern-left",
      "type": "custom",
      "position": { "x": 40, "y": 30, "width": 60, "height": 80 },
      "render": "drawLantern"
    },
    {
      "id": "lantern-right",
      "type": "custom",
      "position": { "x": 670, "y": 30, "width": 60, "height": 80 },
      "render": "drawLantern"
    }
  ]
}
```

---

## 🖌️ Canvas实现要点

### 1. 绘制顺序（从底到上）

```javascript
1. 绘制背景渐变
2. 绘制顶部灯笼装饰
3. 绘制横批
4. 绘制左对联背景 → 文字
5. 绘制主图（用户上传的图片）
6. 绘制右对联背景 → 文字
7. 绘制标题文字
8. 绘制副标题文字
9. 绘制Logo文字
10. 绘制二维码
```

### 2. 文字换行逻辑

```javascript
// 自动换行（标题和副标题）
function wrapText(ctx, text, maxWidth, maxLines) {
  const lines = [];
  let currentLine = '';

  for (let char of text) {
    const testLine = currentLine + char;
    const metrics = ctx.measureText(testLine);

    if (metrics.width > maxWidth && currentLine) {
      lines.push(currentLine);
      currentLine = char;
      if (lines.length >= maxLines - 1) break;
    } else {
      currentLine = testLine;
    }
  }

  if (currentLine && lines.length < maxLines) {
    lines.push(currentLine);
  }

  return lines;
}
```

### 3. 对联垂直文字

```javascript
// 垂直文字渲染
function drawVerticalText(ctx, text, x, y, gap) {
  ctx.save();
  ctx.textAlign = 'center';

  let currentY = y;
  for (let char of text) {
    ctx.fillText(char, x, currentY);
    currentY += gap;
  }

  ctx.restore();
}
```

---

## ⚠️ 关键注意事项

### 1. 遮挡问题已解决
```
✅ 主图宽度: 500px (66.6%)
✅ 对联宽度: 50px (6.6%)
✅ 安全间隙: 15px (2%)

结论: 对联绝不会遮挡主图内容
```

### 2. 排版精准对齐
```
✅ 所有坐标均使用绝对定位
✅ 居中元素使用 x = 375 (750/2)
✅ 垂直间距统一: 20px
✅ 圆角统一: 小元素12px，大元素32px
```

### 3. 品牌色严格使用
```
✅ 主红色: #E53935 (var(--cny-red-500))
✅ 金色: #FFD700 (var(--cny-gold-500))
✅ 不使用草案的 #E63946 和 #D4AF37
```

### 4. 字体使用规范
```
✅ 西文: -apple-system, sans-serif
✅ 中文: "Noto Sans SC", "PingFang SC"
✅ 对联: "Noto Serif SC" (衬线字体更有书法感)
```

---

## 📱 移动端适配

### 响应式缩放
```javascript
// 自动适配屏幕宽度
const screenWidth = window.innerWidth;
const scale = Math.min(screenWidth / 750, 1);

canvas.style.width = `${750 * scale}px`;
canvas.style.height = `${1334 * scale}px`;
```

### 触摸优化
```css
/* 防止长按选择 */
canvas {
  -webkit-user-select: none;
  user-select: none;
  -webkit-touch-callout: none;
}
```

---

## 🎨 参考草案的优点（借鉴）

✅ **对联 + 横批的整体布局** - 传统与现代结合
✅ **玻璃态背景** - 增加层次感
✅ **几何灯笼** - 现代简约风格
✅ **底部专业排版** - Logo + 二维码

---

## 🚫 草案的问题（避免）

❌ **对联遮挡主图** - 已通过空间分配解决
❌ **Tailwind依赖** - 改用CSS变量
❌ **配色偏差** - 使用我们的品牌色
❌ **HTML渲染** - 改用Canvas直接生成图片

---

## 🚀 下一步实施

### Phase 1: Canvas绘制引擎（4小时）
```
1. 创建 PosterGenerator.ts
2. 实现基础绘制方法（背景、装饰、文字、图片）
3. 实现JSON配置驱动的绘制逻辑
4. 测试基础功能
```

### Phase 2: 装饰元素绘制（2小时）
```
1. 实现灯笼绘制（渐变、发光、文字）
2. 实现横批绘制（胶囊形、装饰点）
3. 实现对联背景（白色/红色渐变、印章）
4. 测试装饰效果
```

### Phase 3: 填充内容处理（2小时）
```
1. 图片加载和缩放
2. 文字换行和溢出
3. 二维码生成
4. 测试各种内容场景
```

### Phase 4: 集成和优化（2小时）
```
1. 集成到ResultPage或新建PosterPage
2. 性能优化（生成速度<1秒）
3. 移动端测试
4. 多种内容测试
```

**总计: 10小时（1.5个工作日）**

---

**文档完成时间**: 2026-02-01
**下一步**: 等待确认后开始Phase 1实施
