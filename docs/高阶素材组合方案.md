# 🚀 高阶素材自由组合方案

## 用户需求（完全理解）

### 目标：
**让用户像搭积木一样，自由组合所有素材元素，生成无限种图片**

### 元素库：
- 📸 **人物**（M2换脸后的结果）
- 🖼️ **背景**（34个 + 用户每天新增）
- 🧧 **对联**（30组）
- 🎨 **装饰**（137个：印章、烫金、艺术字、书法）
- ✍️ **文字**（用户自定义）

### 核心功能：
1. **背景替换** - 换脸后不满意背景，换成任意背景
2. **元素叠加** - 添加对联、装饰、文字
3. **无限组合** - 排列组合生成无数新图

---

## 🔑 关键技术：自动抠图（去背景）

### 问题：
M2换脸结果是**完整图片**（人物+背景），如何把人物抠出来？

### 解决方案：AI抠图引擎

---

## 方案A：Rembg（开源免费）⭐ 推荐

### 技术栈：
- Python + U2Net深度学习模型
- 自动识别人物，去除背景
- 输出PNG透明背景

### 集成方式：

#### 1. 安装Rembg服务（后端）

```bash
# 安装Python依赖
pip install rembg[gpu]  # GPU版本（推荐）
# 或
pip install rembg  # CPU版本

# 启动HTTP服务
rembg s --host 0.0.0.0 --port 5000
```

#### 2. Node.js API调用

```typescript
// services/BackgroundRemovalService.ts
import axios from 'axios';
import FormData from 'form-data';

export class BackgroundRemovalService {
  private static REMBG_API = 'http://localhost:5000/api/remove';

  /**
   * 去除图片背景
   * @param imageUrl 原图URL
   * @returns PNG透明背景图URL
   */
  static async removeBackground(imageUrl: string): Promise<string> {
    try {
      // 下载原图
      const imageBlob = await fetch(imageUrl).then(r => r.blob());

      // 调用Rembg API
      const formData = new FormData();
      formData.append('file', imageBlob, 'image.jpg');

      const response = await axios.post(this.REMBG_API, formData, {
        headers: formData.getHeaders(),
        responseType: 'arraybuffer'
      });

      // 转换为Base64
      const base64 = Buffer.from(response.data, 'binary').toString('base64');
      const dataUrl = `data:image/png;base64,${base64}`;

      return dataUrl;
    } catch (error) {
      console.error('背景去除失败:', error);
      throw new Error('抠图失败');
    }
  }
}
```

#### 3. 前端使用

```typescript
// 用户点击"替换背景"按钮
async function handleReplaceBackground(resultImageUrl: string) {
  // 1. 显示加载中
  message.loading('正在抠图，请稍候...');

  // 2. 调用抠图服务
  const personOnly = await BackgroundRemovalService.removeBackground(resultImageUrl);

  // 3. 打开编辑器，加载透明人物
  setPersonImage(personOnly);  // PNG透明背景
  setShowEditor(true);

  message.success('抠图完成！现在可以自由组合背景了');
}
```

#### 优点：
- ✅ 免费开源
- ✅ 效果好（U2Net模型）
- ✅ 本地部署，数据安全
- ✅ 速度快（GPU: 1-2秒/张）

#### 缺点：
- ❌ 需要部署Python服务
- ❌ 需要GPU（CPU较慢）

---

## 方案B：Remove.bg API（商用SaaS）

### 技术栈：
- 商用API，无需部署
- 效果优秀，速度快

### 集成方式：

```typescript
// services/BackgroundRemovalService.ts
import axios from 'axios';

export class BackgroundRemovalService {
  private static REMOVE_BG_API = 'https://api.remove.bg/v1.0/removebg';
  private static API_KEY = process.env.REMOVE_BG_API_KEY;

  static async removeBackground(imageUrl: string): Promise<string> {
    try {
      const response = await axios.post(
        this.REMOVE_BG_API,
        {
          image_url: imageUrl,
          size: 'auto'
        },
        {
          headers: {
            'X-Api-Key': this.API_KEY
          },
          responseType: 'arraybuffer'
        }
      );

      const base64 = Buffer.from(response.data, 'binary').toString('base64');
      return `data:image/png;base64,${base64}`;
    } catch (error) {
      throw new Error('抠图失败');
    }
  }
}
```

#### 优点：
- ✅ 无需部署，开箱即用
- ✅ 效果顶级
- ✅ 速度极快（<1秒）

#### 缺点：
- ❌ 收费（$0.20/张，前50张免费）
- ❌ 需要网络请求

---

## 方案C：浏览器端抠图（MediaPipe Selfie Segmentation）

### 技术栈：
- Google MediaPipe
- 纯前端，无需后端
- WebAssembly + TensorFlow.js

### 集成方式：

```typescript
// services/BrowserBackgroundRemoval.ts
import { SelfieSegmentation } from '@mediapipe/selfie_segmentation';

export class BrowserBackgroundRemoval {
  private segmentation: SelfieSegmentation;

  constructor() {
    this.segmentation = new SelfieSegmentation({
      locateFile: (file) => {
        return `https://cdn.jsdelivr.net/npm/@mediapipe/selfie_segmentation/${file}`;
      }
    });

    this.segmentation.setOptions({
      modelSelection: 1,  // 0=通用, 1=风景画
      selfieMode: false
    });
  }

  async removeBackground(imageUrl: string): Promise<string> {
    return new Promise((resolve) => {
      const img = new Image();
      img.crossOrigin = 'anonymous';
      img.src = imageUrl;

      img.onload = () => {
        this.segmentation.onResults((results) => {
          const canvas = document.createElement('canvas');
          canvas.width = img.width;
          canvas.height = img.height;
          const ctx = canvas.getContext('2d')!;

          // 应用遮罩
          ctx.drawImage(results.segmentationMask, 0, 0);
          ctx.globalCompositeOperation = 'source-in';
          ctx.drawImage(results.image, 0, 0);

          resolve(canvas.toDataURL('image/png'));
        });

        this.segmentation.send({ image: img });
      };
    });
  }
}
```

#### 优点：
- ✅ 完全免费
- ✅ 纯前端，无后端依赖
- ✅ 隐私安全（不上传服务器）

#### 缺点：
- ❌ 效果一般（不如专业AI模型）
- ❌ 速度较慢（3-5秒）
- ❌ 需要下载模型文件（~10MB）

---

## 🎨 完整工作流程

### 流程图：

```
M2换脸完成
    ↓
ResultPage显示结果图（完整图，人物+背景A）
    ↓
【用户点击"高级编辑"按钮】
    ↓
弹窗选择：
  选项1: 直接编辑（叠加装饰，不换背景）
  选项2: 高级编辑（自由组合，可换背景）
    ↓
【选项2: 高级编辑】
    ↓
调用抠图服务（Rembg/Remove.bg/MediaPipe）
    ↓
显示"抠图中..."进度条（1-5秒）
    ↓
抠图完成 → 得到PNG透明人物
    ↓
打开AdvancedComposerEditor
    ↓
Canvas初始化（空白画布）
    ↓
用户自由组合：
  1. 选择背景（34个背景）
  2. 添加人物（自动居中）
  3. 调整人物（缩放、位置）
  4. 添加对联（30组）
  5. 添加装饰（137个）
  6. 添加文字（自定义）
    ↓
实时预览（Canvas渲染）
    ↓
导出最终图片（2048x2048 PNG）
    ↓
保存到素材库 / 下载 / 生成视频
```

---

## 💻 关键代码实现

### 1. AdvancedComposerEditor组件

```typescript
// components/AdvancedComposerEditor.tsx
import React, { useState, useEffect } from 'react';
import { fabric } from 'fabric';
import { BackgroundRemovalService } from '../services/BackgroundRemovalService';
import { message } from 'antd';

interface AdvancedComposerEditorProps {
  originalImage: string;  // M2换脸结果图
  onClose: () => void;
  onSave: (finalImage: string) => void;
}

export const AdvancedComposerEditor: React.FC<AdvancedComposerEditorProps> = ({
  originalImage,
  onClose,
  onSave
}) => {
  const [canvas, setCanvas] = useState<fabric.Canvas | null>(null);
  const [personImage, setPersonImage] = useState<string>('');
  const [isRemoving, setIsRemoving] = useState(false);

  // 初始化：自动抠图
  useEffect(() => {
    async function init() {
      setIsRemoving(true);
      message.loading('正在智能抠图，请稍候...', 0);

      try {
        // 抠图
        const transparentPerson = await BackgroundRemovalService.removeBackground(originalImage);
        setPersonImage(transparentPerson);

        // 初始化Canvas
        const fabricCanvas = new fabric.Canvas('advanced-canvas', {
          width: 1024,
          height: 1024
        });
        setCanvas(fabricCanvas);

        message.destroy();
        message.success('抠图完成！开始自由组合吧');
      } catch (error) {
        message.destroy();
        message.error('抠图失败，请重试');
      } finally {
        setIsRemoving(false);
      }
    }

    init();
  }, [originalImage]);

  // 添加背景
  const addBackground = (bgUrl: string) => {
    if (!canvas) return;

    fabric.Image.fromURL(bgUrl, (img) => {
      img.set({
        selectable: false,
        scaleX: canvas.width / img.width,
        scaleY: canvas.height / img.height
      });

      // 移除旧背景
      const oldBg = canvas.getObjects().find(obj => obj.data?.type === 'background');
      if (oldBg) canvas.remove(oldBg);

      img.data = { type: 'background' };
      canvas.add(img);
      canvas.sendToBack(img);
      canvas.renderAll();
    });
  };

  // 添加人物（透明背景）
  const addPerson = () => {
    if (!canvas || !personImage) return;

    fabric.Image.fromURL(personImage, (img) => {
      img.set({
        left: canvas.width / 2 - img.width / 2,
        top: canvas.height / 2 - img.height / 2,
        selectable: true
      });

      canvas.add(img);
      canvas.renderAll();
    });
  };

  // 导出
  const handleExport = () => {
    if (!canvas) return;

    const dataURL = canvas.toDataURL({
      format: 'png',
      quality: 1.0,
      multiplier: 2
    });

    onSave(dataURL);
  };

  return (
    <div className="advanced-composer-editor">
      {isRemoving ? (
        <div className="removing-bg">
          <div className="spinner"></div>
          <p>正在智能抠图...</p>
        </div>
      ) : (
        <>
          <div className="editor-sidebar">
            <h3>🖼️ 背景（34个）</h3>
            <div className="bg-grid">
              {BACKGROUND_TEMPLATES.map(bg => (
                <img
                  key={bg.id}
                  src={bg.imagePath}
                  onClick={() => addBackground(bg.imagePath)}
                />
              ))}
            </div>

            <h3>👤 人物</h3>
            <button onClick={addPerson}>添加人物到画布</button>

            <h3>🧧 对联（30组）</h3>
            {/* 对联选择... */}

            <h3>🎨 装饰（137个）</h3>
            {/* 装饰选择... */}
          </div>

          <div className="editor-canvas">
            <canvas id="advanced-canvas"></canvas>
          </div>

          <div className="editor-toolbar">
            <button onClick={handleExport}>💾 保存</button>
            <button onClick={onClose}>❌ 关闭</button>
          </div>
        </>
      )}
    </div>
  );
};
```

---

## 📊 成本估算

### 方案A：Rembg（本地部署）
- **硬件成本**: GPU服务器（如阿里云GPU实例：¥2-5/小时）
- **或自建服务器**: RTX 3060（¥2000-3000一次性）
- **运营成本**: ¥0/张（无限制）

### 方案B：Remove.bg
- **API成本**: $0.20/张（约¥1.4/张）
- **免费额度**: 前50张免费
- **月成本**: 1000张 = ¥1400

### 方案C：MediaPipe（纯前端）
- **成本**: ¥0（完全免费）
- **用户体验**: 略差（速度慢3-5秒）

---

## 🎯 推荐方案

### 阶段1：立即可用（今天）
使用**MediaPipe浏览器端抠图**
- 免费
- 无需部署
- 效果可接受
- 快速验证需求

### 阶段2：优化体验（1周内）
部署**Rembg本地服务**
- GPU服务器（阿里云按需付费）
- 效果更好
- 速度更快（1-2秒）

### 阶段3：商业化（长期）
集成**Remove.bg API**（备用）
- 作为备用方案
- 高峰期分流
- 保证服务质量

---

## 🚀 技术优势

✅ **无限组合** - 背景 × 人物 × 对联 × 装饰 = 无限种图片
✅ **自动抠图** - AI智能去背景，1-5秒完成
✅ **实时预览** - Canvas实时渲染，所见即所得
✅ **高清导出** - 2048x2048分辨率
✅ **素材持续增长** - 用户每天新增素材，价值越来越大

---

## 📋 立即行动

### 我现在就做：

1. ✅ **创建BackgroundRemovalService**（抠图服务）
2. ✅ **创建AdvancedComposerEditor组件**（高级编辑器）
3. ✅ **集成MediaPipe浏览器端抠图**（立即可用）
4. ✅ **在ResultPage添加"高级编辑"按钮**

### 您需要决定：

1. **抠图方案选择**：
   - 方案A: MediaPipe（免费，今天就能用）
   - 方案B: Rembg（需部署Python服务）
   - 方案C: Remove.bg（需API付费）

2. **是否接受3-5秒抠图等待时间**？
   - 接受 → 用MediaPipe
   - 不接受 → 需部署Rembg或用Remove.bg

---

## 总结

**您的背景素材完全没白买！**

核心技术：
1. **AI抠图** - 把人物从原背景中抠出来
2. **Canvas自由组合** - 背景 + 人物 + 装饰，无限排列
3. **实时预览** - 所见即所得
4. **高清导出** - 2048x2048

**完全可行！技术成熟！** ✅

现在告诉我您选择哪个抠图方案，我立即开始写代码！
